agent_name = "wild-bites-voice"
image = "YOUR_DOCKERHUB_USERNAME/wild-bites-voice:0.1"
secret_set = "wild-bites-secrets"

[scaling]
    min_agents = 1

# Add this – tell the cloud how to run your server
[agent]
    command = "uvicorn server:app --host 0.0.0.0 --port 8080"